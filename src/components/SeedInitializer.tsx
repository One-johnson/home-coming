"use client";

/**
 * Public auto-seed is disabled — content seeding is admin-only via the dashboard.
 * Kept so layout imports remain stable.
 */
export function SeedInitializer() {
  return null;
}
